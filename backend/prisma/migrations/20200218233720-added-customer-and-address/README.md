# Migration `20200218233720-added-customer-and-address`

This migration has been generated by Rostislav Klein at 2/18/2020, 11:37:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `db`.`Customer` (
    `allowedBankPayments` boolean NOT NULL DEFAULT false ,
    `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    `createdBy` varchar(191) NOT NULL ,
    `email` varchar(191)   ,
    `id` varchar(191) NOT NULL  ,
    `identificationNumber` varchar(191)   ,
    `name` varchar(191)   ,
    `personName` varchar(191)   ,
    `phone` varchar(191)   ,
    `taxIdentificationNumber` varchar(191)   ,
    `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `db`.`Address` (
    `city` varchar(191)   ,
    `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    `createdBy` varchar(191) NOT NULL ,
    `customer` varchar(191)  ,
    `id` varchar(191) NOT NULL  ,
    `number` varchar(191)   ,
    `postNumber` varchar(191)   ,
    `state` varchar(191)   ,
    `street` varchar(191)   ,
    `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `db`.`Order` ADD COLUMN `customer` varchar(191)  ;

ALTER TABLE `db`.`Customer` ADD FOREIGN KEY (`createdBy`) REFERENCES `db`.`User`(`id`) ON DELETE RESTRICT

ALTER TABLE `db`.`Address` ADD FOREIGN KEY (`createdBy`) REFERENCES `db`.`User`(`id`) ON DELETE RESTRICT

ALTER TABLE `db`.`Address` ADD FOREIGN KEY (`customer`) REFERENCES `db`.`Customer`(`id`) ON DELETE SET NULL

ALTER TABLE `db`.`Order` ADD FOREIGN KEY (`customer`) REFERENCES `db`.`Customer`(`id`) ON DELETE SET NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200207002634-init..20200218233720-added-customer-and-address
--- datamodel.dml
+++ datamodel.dml
@@ -6,14 +6,14 @@
 // The `url` field must contain the connection string to your DB.
 // Learn more about connection strings for your DB: https://pris.ly/connection-strings
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 // Other examples for connection strings are:
-// SQLite: url = "***"
-// MySQL:  url = "***"
+// SQLite: url = "sqlite:./dev.db"
+// MySQL:  url = "mysql://johndoe:johndoe@localhost:3306/mydb"
 // You can also use environment variables to specify the connection string: https://pris.ly/prisma-schema#using-environment-variables
 // By adding the `generator` block, you specify that you want to generate Prisma's DB client.
 // The client is generated by runnning the `prisma generate` command and will be located in `node_modules/@prisma` and can be imported in your code as:
@@ -41,10 +41,38 @@
   name     String?
 }
 model Order {
-  id        String   @id @default(cuid())
-  number    Int      @unique
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
+  id        String    @id @default(cuid())
+  number    Int       @unique
+  customer  Customer?
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
   createdBy User
+}
+
+model Customer {
+  id                      String    @id @default(cuid())
+  name                    String?
+  personName              String?
+  phone                   String?
+  email                   String?
+  identificationNumber    String?
+  taxIdentificationNumber String?
+  allowedBankPayments     Boolean   @default(false)
+  address                 Address[]
+  createdBy               User
+  createdAt               DateTime  @default(now())
+  updatedAt               DateTime  @updatedAt
+}
+
+model Address {
+  id         String   @id @default(cuid())
+  street     String?
+  number     String?
+  city       String?
+  state      String?
+  postNumber String?
+  createdBy  User
+  createdAt  DateTime @default(now())
+  updatedAt  DateTime @updatedAt
 }
```


